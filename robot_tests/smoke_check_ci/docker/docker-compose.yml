version: '3.3'
services:
    smoke_check:
        network_mode: bridge
        image: cloud_portal_smoke_check
        shm_size: "256M"

#    mediaserver:
#        network_mode: bridge
#        image: mediaserver
#        shm_size: "256M"

{
  "name": "request-interceptor-example",
  "description": "Request Interceptor Example",
  "private": true,
  "includeInDemo": true,
  "version": "0.0.0",
  "license": "MPL-2.0",
  "type": "module",
  "scripts": {
    "start": "nx run request-interceptor-example:serve",
    "serve": "vite",
    "build:demo": "tsc --outDir ../dist/request-interceptor-example && vite build --outDir ../dist/request-interceptor-example --emptyOutDir --base ./ && cp ./package.json ../dist/request-interceptor-example/package.json",
    "deploy:demo": "echo 'FUTURE TODO: Add UI wrapper to display show all buildable demos and deploy them statically'",
    "ci:check": "echo 'Additional CI checks can be added here'"
  },
  "dependencies": {
    "@networkoptix/request-interceptor": "*"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.2.0"
  }
}
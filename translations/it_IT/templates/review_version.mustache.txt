C'è una nuova versione per {{ message.asset }} ({{message.asset_type}}) da revisionare.

Clicchi sul link per rivedere il progetto {{ message.id }} per {{ message.asset }}

Link per la Revisione: {{ config.portal_url }}/admin/cms/version/{{ message.id }}/

Su questa pagina è possibile
1) Creare un'anteprima per visualizzare le modifiche più recenti.
2) Pubblica questa versione che aggiornerà tutto il contenuto.
3) modificare contesti facendo clic sul pulsante di registrazione di modifica. Per vedere queste modifiche è necessario creare una nuova versione.

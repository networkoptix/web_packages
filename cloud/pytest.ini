[pytest]
DJANGO_SETTINGS_MODULE = cloud.settings
python_files = test_*.py *_tests.py
addopts = --reuse-db --maxprocesses=3
env =
    D:TESTING = True
    D:LOCAL_ENV = True
markers =
    integration
    slow
    no_db
asyncio_mode = auto

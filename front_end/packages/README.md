This folder should contain publishable packages.

The existing ones should be refactored to use nx publishable libraries.

Any new publishable libraries should go here.

Publishable libraries could include internal tools that we want to published to our private repo or sharable packages that we want to publish to npm.
export enum SessionState {
    Detach = 'detach',
    Disconnect = 'disconnect',
    Merge = 'merge',
    RenewWeb = 'renewWeb',
    Reset = 'reset',
    Restart = 'restart',
    Transfer = 'transfer',
}

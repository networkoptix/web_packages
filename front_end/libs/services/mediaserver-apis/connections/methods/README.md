This will be where we include classes that handle different connection methods to the server.

Initially just handle websocket json-rpc and angular http client.

Once we are done with the system api refactor we'll probably want to add an http fetch method also.
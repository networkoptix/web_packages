We'll define concrete connection implementations at the root of this folder.

Most concrete classes will be composed from a connection method instance and an appropriate adapter.

Eventually we'll want to  create factories to dynamically compose classes but for our current use case we don't need it.
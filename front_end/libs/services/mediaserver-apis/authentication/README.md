We'll define concrete authentication implementations at the root of this folder.

Most concrete classes will be composed from an authentication method instance and an appropriate adapter.

I don't think we'll ever need to create factories to dynamically create instances.
/**
 * TODO: Create theme selector component
 * @param template
 * @returns
 */
export const withThemeSelector = (template: string): string => `
    ${template}
    <button>Toggle Theme Selector</button>
`;

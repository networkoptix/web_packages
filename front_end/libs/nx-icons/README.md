# nx-icons

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nx-icons` to build the library.

## Running unit tests

Run `nx test nx-icons` to execute the unit tests via [Jest](https://jestjs.io).

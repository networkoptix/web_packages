{
  "name": "dashboard-widget-state",
  "version": "0.4.2",
  "description": "Shared state for use with dashboard widgets",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "ts-types": "tsc --emitDeclarationOnly || true",
    "build": "rimraf dist && node ./esbuild.js && npm run ts-types",
    "prelink": "npm run build",
    "prepare": "npm run build",
    "postpack": "rimraf dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.21",
    "rimraf": "^3.0.2",
    "tsc": "^2.0.4",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "rxjs": "^7.5.4"
  }
}

import colors from '../drawingConfigs/colors';

import { deg, px, pxPerSecond, color } from './types';

export default {
    stripeWidth: 8 as px,
    slope: 45 as deg,
    speed: 24 as pxPerSecond,
    backgroundColor: `${colors.light6}C0` as color,
    stripeColor: `${colors.light1}C0` as color
};

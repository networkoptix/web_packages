* continue moving `interval` field into Rulers (`MultiWeight*`) DONE
* support `interval/weight-dependent dateformats` DONE
* introduce transition animations TODO
* introduce `MIN_WIDTH` into naming as `singleWidth` (`naive`?) vs. `interval-dependent width` (?) WONTFIX

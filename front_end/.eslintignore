# don't ever lint node_modules
node_modules

# don't lint build output (make sure it's set to your correct build folder name)
dist
build

# don't lint nyc coverage output
coverage

# Don't lint tsconfig
tsconfig.json

# Don't lint deprecated files/folders
*.bak.*
**/*.bak/**/*
*.spec-jest.ts

# Don't lint example file
vendor.ts

# Don't lint environment files
environments

# Code copied from original package and patched
libs/ngx-translate-patch/**

# Keeping as reference while refactoring view, will eventually delete
libs/features/systems/view/datatypes/TestCamera.ts
libs/features/systems/view/datatypes/testMediaServers.ts

apps/**/*.d.ts
**/*.example.ts

# Ignore until we can update lint config for react
libs/nx-components/.storybook/**

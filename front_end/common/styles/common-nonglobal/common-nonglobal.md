# `styles/common-nonglobal/`

This folder is for de-globalized styles from `styles/common/` during conversion to
Cloud 2.0 design. It has not been added to absolute paths for SCSS files, use the folder name
for paths:

```scss
@use 'common-nonglobal/button-legacy';
```

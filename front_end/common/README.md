This should contain all code and assets that are shared across the project.

This is code that isn't independantly buildable or packagable. If the code be a buildable library it should go in libs, if it's packagable it should go to packages.

Examples would be that once all libary and app code has been moved from the src folder the remaining contents would go here.
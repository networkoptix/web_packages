This folder will contain self contain features(prevoiusly pages) and should be decoupled so that they can be run independantly.

When we add module federation you'd be able to run an application with just the app shell and selected features being served with the rest of the app using cached feature modules.
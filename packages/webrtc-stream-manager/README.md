# @networkoptix/webrtc-stream-manager-v1-legacy (v1 source — not published)

This directory holds v1 source retained **for the internal
`useWebRTCStreamManagerV2` feature-flag branch in cloud_portal/front_end**.

The published `@networkoptix/webrtc-stream-manager` package on npm is built
from the sibling `open/packages/webrtc-stream-manager-v2/` directory starting
at version `0.1.29`. See that directory's README for public API docs,
installation instructions, and migration notes.

This package is marked `"private": true` and must not be published.
